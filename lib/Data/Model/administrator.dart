class Administrator {
  final String uid, adminDocId, firstName, lastName, email;

  Administrator(
      {required this.adminDocId,
      required this.uid,
      required this.firstName,
      required this.lastName,
      required this.email});
}
